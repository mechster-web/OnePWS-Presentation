import { PlaceholderScene } from "./PlaceholderScene";
import type { SceneComponentProps } from "./SceneTypes";

export function CinematicOpeningScene(props: SceneComponentProps) {
  return <PlaceholderScene {...props} />;
}
